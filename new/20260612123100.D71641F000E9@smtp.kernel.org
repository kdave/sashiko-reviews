From sashiko-bot@kernel.org Fri Jun 12 14:31:08 2026
From: sashiko-bot@kernel.org
Subject: Re: [PATCH 1/2] btrfs: fix reloc root cleanup in
 merge_reloc_roots()
Date: Fri, 12 Jun 2026 12:31:00 +0000
Message-Id: <20260612123100.D71641F000E9@smtp.kernel.org>

Thank you for your contribution! Sashiko AI review found 1 potential issue(s) to consider:
- [Low] The patch calls an undefined function `clear_reloc_root()`, which causes a build failure.
--

commit bb4750ad98e8ace7b16e78587e59a04699952e7e
Author: Filipe Manana <fdmanana@suse.com>

btrfs: fix reloc root cleanup in merge_reloc_roots()

This commit updates merge_reloc_roots() to use clear_reloc_root() for
resetting the root's reloc_root pointer. This ensures proper memory barriers
are issued before clearing the BTRFS_ROOT_DEAD_RELOC_TREE bit.

Link: https://sashiko.dev/#/patchset/cf84f1a217c719e25b6b69e4298dd7afd36c9427.1781194426.git.fdmanana%40suse.com

> diff --git a/fs/btrfs/relocation.c b/fs/btrfs/relocation.c
> index 3ebaf5880125f..a5a8c974ba7c7 100644
> --- a/fs/btrfs/relocation.c
> +++ b/fs/btrfs/relocation.c
> @@ -1863,11 +1863,10 @@ void merge_reloc_roots(struct reloc_control *rc)
>  		} else {
>  			if (!IS_ERR(root)) {
>  				if (root->reloc_root == reloc_root) {
> -					root->reloc_root = NULL;
> +					clear_reloc_root(root);

[Severity: Low]
Is clear_reloc_root() defined anywhere in this patch or earlier in the
series? It appears this function is called here in merge_reloc_roots()
but not actually implemented or declared, which will cause a build failure
due to an implicit function declaration.

> +					/* Drop the ref for root->reloc_root. */
>  					btrfs_put_root(reloc_root);
>  				}
> -				clear_bit(BTRFS_ROOT_DEAD_RELOC_TREE,
> -					  &root->state);
>  				btrfs_put_root(root);
>  			}

-- 
Sashiko AI review · https://sashiko.dev/#/patchset/cover.1781263239.git.fdmanana@suse.com?part=1

