From sashiko-bot@kernel.org Tue Aug  4 18:24:40 2026
From: sashiko-bot@kernel.org
Subject: Re: [PATCH v2 3/5] btrfs: pre-allocate delayed dir index before
 btree modification
Date: Tue, 04 Aug 2026 16:24:29 +0000
Message-Id: <20260804162430.53A081F000E9@smtp.kernel.org>

Thank you for your contribution! Sashiko AI review found 3 potential issue(s) to consider:

New issues:
- [High] btrfs_insert_dir_item violates its ownership transfer contract for the prealloc parameter, leaking memory and resources.
- [Medium] Caller-provided `prealloc` structs lead to uninitialized memory in delayed dir index filenames.

Pre-existing issues:
- [High] Orphaned delayed `DIR_INDEX` item created when `DIR_ITEM` insertion fails with `-EEXIST`.
--

--- Patch [3]: [PATCH v2 3/5] btrfs: pre-allocate delayed dir index before btree modification ---
commit 93d41aa962f28136a077ed156c86f870d201caf4
Author: Jeff Layton <jlayton@kernel.org>

btrfs: pre-allocate delayed dir index before btree modification

This commit moves the delayed dir index allocation in btrfs_insert_dir_item()
before the insert_with_overflow() call that modifies the btree. It also adds
an optional caller-provided prealloc parameter to btrfs_insert_dir_item().

> diff --git a/fs/btrfs/dir-item.c b/fs/btrfs/dir-item.c
> index 84f1c64423d32..1b956df2c5718 100644
> --- a/fs/btrfs/dir-item.c
> +++ b/fs/btrfs/dir-item.c
> @@ -106,8 +106,11 @@ int btrfs_insert_xattr_item(struct btrfs_trans_handle *trans,
>   * Will return 0 or -ENOMEM
>   */
>  int btrfs_insert_dir_item(struct btrfs_trans_handle *trans,
> -			  const struct fscrypt_str *name, struct btrfs_inode *dir,
> -			  const struct btrfs_key *location, u8 type, u64 index)
> +			  const struct fscrypt_str *name,
> +			  struct btrfs_inode *dir,
> +			  const struct btrfs_key *location, u8 type,
> +			  u64 index,
> +			  struct btrfs_dir_index_prealloc *prealloc)
>  {
>  	int ret = 0;
>  	int ret2 = 0;

[ ... ]

> @@ -130,6 +135,18 @@ int btrfs_insert_dir_item(struct btrfs_trans_handle *trans,
>  
>  	btrfs_cpu_key_to_disk(&disk_key, location);
>  
> +	/* Pre-allocate the delayed dir index before modifying the btree. */
> +	if (need_delayed_index && !prealloc) {
> +		ret = btrfs_prealloc_delayed_dir_index(dir, name->name,
> +						       name->len,
> +						       &local_prealloc);
> +		if (ret)
> +			return ret;
> +		memcpy(local_prealloc.item->data + sizeof(struct btrfs_dir_item),
> +		       name->name, name->len);
> +		prealloc = &local_prealloc;
> +	}
> +

[Severity: High]
The commit message states that "ownership of the prealloc transfers to
btrfs_insert_dir_item()".

However, if btrfs_alloc_path() fails earlier in this function:

	path = btrfs_alloc_path();
	if (!path)
		return -ENOMEM;

Does returning -ENOMEM here leak the caller-provided prealloc memory?

[Severity: Medium]
If a caller passes a preallocated struct here, the data memcpy is bypassed.

Looking at btrfs_prealloc_delayed_dir_index(), it uses the name_len for
allocation sizing but does not copy the filename string into the buffer.
Does this require callers to manually initialize the memory to avoid writing
uninitialized garbage data to disk as the directory index filename?

>  	data_size = sizeof(*dir_item) + name->len;
>  	dir_item = insert_with_overflow(trans, root, path, &key, data_size,
>  					name->name, name->len);
> @@ -137,6 +154,8 @@ int btrfs_insert_dir_item(struct btrfs_trans_handle *trans,
>  		ret = PTR_ERR(dir_item);
>  		if (ret == -EEXIST)
>  			goto second_insert;
> +		if (need_delayed_index)
> +			btrfs_free_delayed_dir_index_prealloc(trans, prealloc);
>  		goto out_free;
>  	}

[Severity: High]
If insert_with_overflow() fails, the cleanup is conditionally guarded by
need_delayed_index.

If a caller provides a prealloc structure when modifying the tree root
(where need_delayed_index is false), does this skip the cleanup and
leak the caller-provided memory?

[ ... ]

> @@ -154,15 +173,14 @@ int btrfs_insert_dir_item(struct btrfs_trans_handle *trans,
>  	write_extent_buffer(leaf, name->name, name_ptr, name->len);
>  
>  second_insert:
> -	/* FIXME, use some real flag for selecting the extra index */
> -	if (root == root->fs_info->tree_root) {
> +	if (!need_delayed_index) {
>  		ret = 0;
>  		goto out_free;
>  	}

[Severity: High]
In this success path, if need_delayed_index is false and the caller
provided a prealloc struct, does returning early here fail to consume
or free the prealloc, resulting in a resource leak?

>  	btrfs_release_path(path);
>  
> -	ret2 = btrfs_insert_delayed_dir_index(trans, name->name, name->len, dir,
> -					      &disk_key, type, index);
> +	ret2 = btrfs_insert_delayed_dir_index_prealloc(trans, dir, prealloc,
> +						       &disk_key, type, index);
>  out_free:
>  	if (ret)
>  		return ret;

[Severity: High]
This is a pre-existing issue, but if insert_with_overflow() fails with
-EEXIST, the code jumps to second_insert.

When that happens, btrfs_insert_delayed_dir_index_prealloc() inserts the
delayed DIR_INDEX item, and then the original -EEXIST error is returned
via the if (ret) check at out_free.

Does this create an orphaned delayed DIR_INDEX item without a corresponding
DIR_ITEM, leading to a metadata inconsistency when the caller aborts their
operation due to the error?

-- 
Sashiko AI review · https://sashiko.dev/#/patchset/20260804-btrfs-enomem-v2-0-4d923170e8c1@kernel.org?part=3

