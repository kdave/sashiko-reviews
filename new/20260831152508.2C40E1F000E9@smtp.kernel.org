From sashiko-bot@kernel.org Mon Aug 31 17:25:25 2026
From: sashiko-bot@kernel.org
Subject: Re: [PATCH AUTOSEL 6.18-5.10] btrfs: fix reloc root cleanup in
 merge_reloc_roots()
Date: Mon, 31 Aug 2026 15:25:07 +0000
Message-Id: <20260831152508.2C40E1F000E9@smtp.kernel.org>

Thank you for your contribution! Sashiko AI review found 1 potential issue(s) to consider:
- [Low] The patch introduces a call to `clear_reloc_root(root);`, but this function is not defined anywhere in the kernel tree.
--

commit 4b033049e47560eb441ff4eb515bcef22d6ed613
Author: Filipe Manana <fdmanana@suse.com>

btrfs: fix reloc root cleanup in merge_reloc_roots()

This commit updates the cleanup of a root's reloc_root in merge_reloc_roots()
when the root has zero root refs, using a memory barrier to prevent state
inconsistencies. It replaces a direct NULL assignment and clear_bit call with
a helper function.

Link: https://sashiko.dev/#/patchset/cf84f1a217c719e25b6b69e4298dd7afd36c9427.1781194426.git.fdmanana%40suse.com

> diff --git a/fs/btrfs/relocation.c b/fs/btrfs/relocation.c
> index 0765e06d00b80..89de454839680 100644
> --- a/fs/btrfs/relocation.c
> +++ b/fs/btrfs/relocation.c
> @@ -1870,11 +1870,10 @@ void merge_reloc_roots(struct reloc_control *rc)
>  		} else {
>  			if (!IS_ERR(root)) {
>  				if (root->reloc_root == reloc_root) {
> -					root->reloc_root = NULL;
> +					clear_reloc_root(root);

[Severity: Low]
Does this cause a build failure? The function clear_reloc_root() does not
appear to be defined anywhere in the tree. Could this patch be relying on a
helper introduced in a separate patch series that is currently missing?

> +					/* Drop the ref for root->reloc_root. */
>  					btrfs_put_root(reloc_root);
>  				}
> -				clear_bit(BTRFS_ROOT_DEAD_RELOC_TREE,
> -					  &root->state);
>  				btrfs_put_root(root);
>  			}

-- 
Sashiko AI review · https://sashiko.dev/#/patchset/20260831133314.4125787-275-sashal@kernel.org?part=1

